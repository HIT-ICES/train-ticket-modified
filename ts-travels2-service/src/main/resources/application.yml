# HTTP Server
server:
  port: 16346  # HTTP (Tomcat) port

mvf4ms:
  version: 1.0.0
spring:
  application:
    name: ts-travels2-service
  data:
    mongodb:
      host: ts-travel2-mongo
#      username: user
#      password: ${MONGODB_PASSWORD}
      database: ts
      port: 27017
swagger:
  controllerPackage: travels2.controller